cc_defaults {
    name: "llvm-lib-defaults",
    defaults: ["llvm-defaults"],
    export_header_lib_headers: ["llvm-headers"],
    target: {
        windows: {
            enabled: true,
        },
    },
}

subdirs = ["*"]
